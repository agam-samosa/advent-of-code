# My Advent of Code solutions

My [Advent of Code](https://adventofcode.com) solutions, written in Rust.

## How to run

In any UNIX-like shell:

```sh
cargo run -p aoc-<YEAR> --bin day-<DAY> < path/to/input
```

For example,

```sh
cargo run -p aoc-2025 --bin day-01 < path/to/input
```
